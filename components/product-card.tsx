import Link from 'next/link';import {Product,formatPrice} from '@/lib/products'
export function ProductCard({p}:{p:Product}){return <Link href={`/producto/${p.slug}`} className="group block"><div className="aspect-[3/4] overflow-hidden bg-white/5"><img src={p.image} alt={p.name} className="product-img h-full w-full object-cover"/></div><div className="mt-4 flex justify-between"><div><p className="text-[10px] uppercase tracking-[.22em] text-gold">{p.category}</p><h3 className="mt-2 font-serif text-2xl">{p.name}</h3></div><p className="text-sm text-white/60">{formatPrice(p.price)}</p></div></Link>}
