'use client'
import Link from 'next/link';import {Menu,ShoppingBag,X} from 'lucide-react';import {useState} from 'react';import {useCart} from './cart-context'
export function Header(){const[menu,setMenu]=useState(false);const{items,setOpen}=useCart();const count=items.reduce((a,b)=>a+b.qty,0);return <header className="fixed inset-x-0 top-0 z-40 border-b border-white/10 bg-black/70 backdrop-blur-xl"><div className="mx-auto flex h-20 max-w-[1440px] items-center justify-between px-6 lg:px-12"><button onClick={()=>setMenu(!menu)} className="lg:hidden" aria-label="Menú"><Menu/></button><Link href="/" className="font-serif text-3xl tracking-[.14em]">D`LOVE</Link><nav className="hidden items-center gap-9 text-[11px] uppercase tracking-[.2em] text-white/70 lg:flex"><Link className="hover:text-gold" href="/coleccion">Colección</Link><Link className="hover:text-gold" href="/historia">Nuestra historia</Link><Link className="hover:text-gold" href="/salud-mental">Compromiso</Link><Link className="hover:text-gold" href="/contacto">Contacto</Link></nav><button onClick={()=>setOpen(true)} className="relative" aria-label="Carrito"><ShoppingBag size={20}/>{count>0&&<span className="absolute -right-3 -top-3 grid h-5 w-5 place-items-center rounded-full bg-gold text-[10px] text-black">{count}</span>}</button></div>{menu&&<div className="absolute inset-x-0 top-0 min-h-screen bg-black p-7"><button onClick={()=>setMenu(false)} className="float-right"><X/></button><div className="mt-24 flex flex-col gap-8 font-serif text-4xl"><Link onClick={()=>setMenu(false)} href="/coleccion">Colección</Link><Link onClick={()=>setMenu(false)} href="/historia">Nuestra historia</Link><Link onClick={()=>setMenu(false)} href="/salud-mental">Compromiso</Link><Link onClick={()=>setMenu(false)} href="/contacto">Contacto</Link></div></div>}</header>}
