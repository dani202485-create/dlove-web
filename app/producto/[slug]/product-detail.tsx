'use client'
import {useState} from 'react';import {Product,formatPrice} from '@/lib/products';import {useCart} from '@/components/cart-context';import {Plus} from 'lucide-react'
export function ProductDetail({p}:{p:Product}){const[size,setSize]=useState('M');const{add}=useCart();return <div className="grid min-h-screen pt-20 lg:grid-cols-[1.2fr_.8fr]"><div className="bg-[#111]"><img src={p.image} alt={p.name} className="h-[65vh] w-full object-cover lg:h-[calc(100vh-5rem)] lg:sticky lg:top-20"/></div><div className="flex items-center px-7 py-16 lg:px-16"><div className="w-full max-w-lg"><p className="text-[10px] uppercase tracking-[.3em] text-gold">{p.category}</p><h1 className="mt-4 font-serif text-5xl md:text-7xl">{p.name}</h1><p className="mt-5 text-lg text-gold">{formatPrice(p.price)}</p><p className="my-9 leading-7 text-white/55">{p.description} Confeccionada en talleres seleccionados y acabada a mano.</p><div className="flex items-center justify-between"><span className="text-xs uppercase tracking-widest">Selecciona tu talla</span><button className="text-xs text-white/40 underline">Guía de tallas</button></div><div className="my-5 grid grid-cols-4 gap-2">{['XS','S','M','L'].map(s=><button onClick={()=>setSize(s)} key={s} className={`border py-4 text-xs ${size===s?'border-gold bg-gold text-black':'border-white/15'}`}>{s}</button>)}</div><button onClick={()=>add(p,size)} className="w-full bg-cream py-5 text-xs uppercase tracking-[.25em] text-black hover:bg-gold">Añadir a la selección</button><div className="mt-10 divide-y divide-white/10 border-y border-white/10">{['Detalles y composición','Envíos y devoluciones','Cuidado de la prenda'].map(x=><button key={x} className="flex w-full items-center justify-between py-5 text-left text-xs uppercase tracking-widest"><span>{x}</span><Plus size={15}/></button>)}</div></div></div></div>}
